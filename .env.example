# Environment variables example for MCREW Bot
# Copy this file to .env and fill with your data

# Master password for decrypting sensitive data
MASTER_PASSWORD=your_secure_master_password_here

# Port for web service (Render.com)
PORT=10000

# Note: All sensitive data (tokens, usernames) are now encrypted in crypto_config.py
# No need to store them in environment variables anymore!