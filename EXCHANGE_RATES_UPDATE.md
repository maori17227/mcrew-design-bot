# 💱 Обновление курсов валют

## ✅ Изменения выполнены

### 1. Убраны Telegram Stars
- ❌ Удалена вкладка Stars из модального окна криптоплатежей
- ❌ Удалены все обработчики Stars платежей
- ❌ Убраны кнопки с суммами Stars
- Теперь только **2 способа оплаты**: TON и USDT

### 2. Фиксированный курс USDT
**1 USDT = 1 ɱ (MTV)**

Это базовый курс, от которого рассчитывается курс TON.

### 3. Автоматическое обновление курса TON

#### Как работает:
1. **API**: Используется CoinGecko API для получения актуальной цены TON в USD
2. **Расчет**: Курс TON к MTV = цена TON в USD (округленная)
3. **Обновление**: Автоматически раз в 24 часа
4. **Кэширование**: Курс сохраняется в localStorage с временной меткой
5. **Fallback**: Если API недоступен, используется кэшированный курс

#### Пример:
- Если 1 TON = $5.23 USD
- То 1 TON ≈ 5 ɱ (округляется до целого)
- Так как 1 USDT = 1 ɱ

#### Где обновляется:
- **Сайт**: В профиле, в модальном окне пополнения
- **Мини-приложение**: В модальном окне криптоплатежей

---

## 📊 Текущие курсы

### Фиксированный курс:
- **1 USDT = 1 ɱ**

### Динамический курс (обновляется каждый день):
- **1 TON ≈ X ɱ** (где X = текущая цена TON в USD)

---

## 🔧 Техническая реализация

### Функция обновления курса:
```javascript
async function updateTONPrice() {
    const response = await fetch(
        'https://api.coingecko.com/api/v3/simple/price?ids=the-open-network&vs_currencies=usd'
    );
    const data = await response.json();
    
    tonToUsdRate = data['the-open-network'].usd;
    tonToMtvRate = Math.floor(tonToUsdRate); // Округление до целого
    
    // Сохранение в localStorage
    localStorage.setItem('ton_rate', JSON.stringify({
        rate: tonToMtvRate,
        usdRate: tonToUsdRate,
        timestamp: Date.now()
    }));
}
```

### Проверка актуальности:
```javascript
function checkAndUpdateRate() {
    const cached = localStorage.getItem('ton_rate');
    const now = Date.now();
    const oneDay = 24 * 60 * 60 * 1000;
    
    if (!cached || (now - cached.timestamp > oneDay)) {
        updateTONPrice(); // Обновить если прошло больше 24 часов
    } else {
        // Использовать кэшированный курс
        tonToMtvRate = cached.rate;
    }
}
```

### Применение курса:
```javascript
// При вводе суммы TON
const mtv = Math.floor(tonAmount * tonToMtvRate);

// При сохранении транзакции
amount: Math.floor(selectedAmount * tonToMtvRate)

// При обновлении баланса
userBalance.mini += Math.floor(selectedAmount * tonToMtvRate);
```

---

## 📱 Где применяется

### Website (mcrew-website.pages.dev)
- ✅ Профиль → Отображение курса
- ✅ Модальное окно пополнения → TON вкладка
- ✅ Расчет суммы при вводе TON
- ✅ Сохранение транзакции с правильной суммой
- ✅ Обновление баланса

### Miniapp (Telegram)
- ✅ Модальное окно криптоплатежей → TON вкладка
- ✅ Отображение актуального курса
- ✅ Расчет суммы при вводе TON
- ✅ Сохранение транзакции с правильной суммой
- ✅ Обновление баланса

---

## 🎯 Преимущества

### 1. Актуальность
- Курс всегда соответствует реальной рыночной цене TON
- Автоматическое обновление без ручного вмешательства

### 2. Простота
- Базовый курс: 1 USDT = 1 ɱ (легко запомнить)
- TON курс рассчитывается автоматически

### 3. Надежность
- Кэширование курса на 24 часа
- Fallback на кэшированное значение при недоступности API
- Не требует постоянного интернет-соединения

### 4. Прозрачность
- Пользователи видят актуальный курс
- Курс обновляется в реальном времени при открытии страницы

---

## 🧪 Тестирование

### Проверить обновление курса:
1. Открыть консоль браузера (F12)
2. Очистить localStorage: `localStorage.removeItem('ton_rate')`
3. Перезагрузить страницу
4. В консоли увидите: `TON rate updated: 1 TON = X.XX USD = X ɱ`

### Проверить кэширование:
1. Открыть страницу (курс загрузится)
2. Проверить localStorage: `localStorage.getItem('ton_rate')`
3. Увидите JSON с курсом и временной меткой
4. Перезагрузить страницу в течение 24 часов
5. Курс загрузится из кэша (без запроса к API)

### Проверить расчет:
1. Открыть модальное окно пополнения TON
2. Ввести сумму (например, 1 TON)
3. Увидеть расчет в MTV (например, "≈ 5 ɱ")
4. Сумма должна соответствовать текущему курсу

---

## 📝 Изменения в файлах

### miniapp/index.html
- Удалена вкладка Stars
- Удалена секция Stars payment
- Добавлен id="ton-rate-miniapp" для динамического обновления курса

### miniapp/app.js
- Удалены обработчики Stars
- Добавлены функции `updateTONPrice()` и `checkAndUpdateRate()`
- Обновлены расчеты TON → MTV с использованием `tonToMtvRate`
- Добавлен вызов `checkAndUpdateRate()` при загрузке

### website/app.js
- Добавлены функции `updateTONPrice()` и `checkAndUpdateRate()`
- Обновлены расчеты TON → MTV с использованием `tonToMtvRate`
- Добавлен вызов `checkAndUpdateRate()` при загрузке
- Обновление отображения курса в профиле

---

## 🚀 Развертывание

**Коммит**: `3466237`
**Сообщение**: "Remove Telegram Stars, add automatic TON rate updates based on real market price (1 USDT = 1 MTV)"

**Статус**: ✅ Отправлено на GitHub
**Автодеплой**: Cloudflare Pages автоматически развернет изменения

---

## 💡 Рекомендации

### Для пользователей:
- Курс TON обновляется автоматически каждый день
- Всегда проверяйте актуальный курс перед пополнением
- 1 USDT всегда равен 1 ɱ

### Для администратора:
- Курс TON можно проверить в консоли браузера
- При необходимости можно вручную обновить: `updateTONPrice()`
- Кэш можно очистить: `localStorage.removeItem('ton_rate')`

---

## 📊 Пример работы

### Сценарий 1: Первое открытие
1. Пользователь открывает сайт/мини-приложение
2. Система запрашивает курс TON у CoinGecko API
3. Получает: 1 TON = $5.23 USD
4. Рассчитывает: 1 TON ≈ 5 ɱ
5. Отображает курс пользователю
6. Сохраняет в кэш с временной меткой

### Сценарий 2: Повторное открытие (в течение 24 часов)
1. Пользователь открывает сайт/мини-приложение
2. Система проверяет кэш
3. Находит актуальный курс (< 24 часов)
4. Загружает из кэша: 1 TON ≈ 5 ɱ
5. Отображает без запроса к API

### Сценарий 3: Открытие через 24+ часов
1. Пользователь открывает сайт/мини-приложение
2. Система проверяет кэш
3. Обнаруживает устаревший курс (> 24 часов)
4. Запрашивает новый курс у API
5. Обновляет отображение и кэш

---

## ✅ Итог

**Telegram Stars удалены** ❌
**Курс USDT фиксирован**: 1 USDT = 1 ɱ ✅
**Курс TON динамический**: Обновляется каждый день по реальной цене ✅
**Автоматизация**: Не требует ручного обновления ✅

Система готова к использованию! 🚀
