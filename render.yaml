services:
  - type: web
    name: mcrew-design-bot-secure
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot_secure.py
    envVars:
      - key: MASTER_PASSWORD
        value: MCREW_SECURE_2024!
      - key: PORT
        value: 10000