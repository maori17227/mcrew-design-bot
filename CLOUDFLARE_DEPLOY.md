# 🚀 Деплой MↃREW бота на Cloudflare Workers

## Почему Cloudflare Workers?

✅ **Бесплатно** - 100,000 запросов в день  
✅ **Быстро** - отклик < 50ms по всему миру  
✅ **Надежно** - 99.99% uptime, никогда не падает  
✅ **Просто** - деплой за 5 минут  

---

## Шаг 1: Установка Wrangler CLI

```bash
npm install -g wrangler
```

Или если у тебя нет npm:
```bash
# Windows (через Chocolatey)
choco install nodejs

# После установки Node.js
npm install -g wrangler
```

---

## Шаг 2: Логин в Cloudflare

```bash
wrangler login
```

Откроется браузер - авторизуйся в Cloudflare (или создай аккаунт, если нет).

---

## Шаг 3: Настройка переменных окружения

В Cloudflare Dashboard:

1. Зайди в **Workers & Pages**
2. Создай новый Worker (или выбери существующий)
3. Перейди в **Settings** → **Variables**
4. Добавь переменные:

```
BOT_TOKEN = "8363446053:AAGfig_At866R3bVU9rNrY4AOuJQxnz_t2M"
ADMIN_CHAT_ID = "твой_chat_id"
```

**Как узнать свой ADMIN_CHAT_ID:**
1. Напиши боту @userinfobot
2. Он пришлет твой chat_id
3. Скопируй число и вставь в переменную

---

## Шаг 4: Деплой бота

```bash
wrangler deploy
```

Готово! Бот задеплоен.

---

## Шаг 5: Установка Webhook

После деплоя получишь URL типа:
```
https://mcrew-bot.your-subdomain.workers.dev
```

Установи webhook командой:

```bash
curl -X POST "https://api.telegram.org/bot8363446053:AAGfig_At866R3bVU9rNrY4AOuJQxnz_t2M/setWebhook?url=https://mcrew-bot.your-subdomain.workers.dev"
```

Или через браузер открой:
```
https://api.telegram.org/bot8363446053:AAGfig_At866R3bVU9rNrY4AOuJQxnz_t2M/setWebhook?url=https://mcrew-bot.your-subdomain.workers.dev
```

Должен вернуться ответ:
```json
{"ok":true,"result":true,"description":"Webhook was set"}
```

---

## Проверка работы

1. Открой бота: https://t.me/mindescrew_bot
2. Напиши `/start`
3. Бот должен ответить мгновенно

---

## Обновление бота

Когда нужно обновить код:

```bash
wrangler deploy
```

Всё! Изменения применятся мгновенно, без перезапуска.

---

## Логи и мониторинг

Смотреть логи в реальном времени:

```bash
wrangler tail
```

Или в Cloudflare Dashboard:
**Workers & Pages** → **твой worker** → **Logs**

---

## Удаление webhook (если нужно вернуться на Render)

```bash
curl -X POST "https://api.telegram.org/bot8363446053:AAGfig_At866R3bVU9rNrY4AOuJQxnz_t2M/deleteWebhook"
```

---

## Преимущества перед Render

| Параметр | Render | Cloudflare Workers |
|----------|--------|-------------------|
| Скорость отклика | 200-500ms | 20-50ms |
| Uptime | 95-98% | 99.99% |
| Холодный старт | 30-60 сек | 0 сек |
| Бесплатный лимит | 750 часов/мес | 100k запросов/день |
| Глобальная сеть | 1 регион | 300+ городов |

---

## Troubleshooting

**Бот не отвечает:**
1. Проверь webhook: `https://api.telegram.org/bot<TOKEN>/getWebhookInfo`
2. Проверь логи: `wrangler tail`
3. Проверь переменные в Cloudflare Dashboard

**Ошибка "env is not defined":**
- Убедись что добавил `BOT_TOKEN` и `ADMIN_CHAT_ID` в Settings → Variables

**Webhook не устанавливается:**
- Проверь что URL начинается с `https://`
- Проверь что worker задеплоен и доступен

---

## Готово! 🎉

Твой бот теперь работает на Cloudflare Workers:
- ⚡ Мгновенные ответы
- 🌍 Работает по всему миру
- 💪 Никогда не падает
- 🆓 Бесплатно

Наслаждайся!
