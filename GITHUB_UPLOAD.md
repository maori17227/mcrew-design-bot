# 📤 Загрузка MↃREW Bot на GitHub

## 🔧 Подготовка (если Git не установлен)

### Установка Git:
1. Скачай Git: https://git-scm.com/download/win
2. Установи с настройками по умолчанию
3. Перезапуsti командную строку

## 🌐 Создание репозитория на GitHub

### 1. Создай аккаунт на GitHub:
- Перейди на https://github.com
- Нажми "Sign up" если нет аккаунта
- Войди в свой аккаунт

### 2. Создай новый репозиторий:
- Нажми зеленую кнопку "New" или "+"
- **Repository name**: `mcrew-design-bot`
- **Description**: `Professional Telegram bot for MↃREW Design Studio`
- Выбери **Public** (или Private если хочешь)
- ✅ Поставь галочку "Add a README file"
- Нажми "Create repository"

## 💻 Загрузка файлов через командную строку

### 3. Открой командную строку в папке с ботом:
- Открой папку с файлами бота в проводнике
- В адресной строке напиши `cmd` и нажми Enter
- Или Shift + правый клик → "Открыть окно команд здесь"

### 4. Выполни команды по порядку:

```bash
# Инициализация Git репозитория
git init

# Настройка пользователя (замени на свои данные)
git config --global user.name "Your Name"
git config --global user.email "your.email@example.com"

# Добавление всех файлов
git add .

# Первый коммит
git commit -m "Initial commit - MCREW Design Bot"

# Переименование ветки в main
git branch -M main

# Подключение к GitHub репозиторию (замени YOUR_USERNAME на свой)
git remote add origin https://github.com/YOUR_USERNAME/mcrew-design-bot.git

# Загрузка на GitHub
git push -u origin main
```

## 🔐 Аутентификация GitHub

### Если попросит логин/пароль:

**Вариант 1 - Personal Access Token (рекомендуется):**
1. Перейди на GitHub → Settings → Developer settings → Personal access tokens
2. Generate new token (classic)
3. Выбери scopes: `repo`, `workflow`
4. Скопируй токен
5. Используй токен вместо пароля

**Вариант 2 - GitHub CLI:**
```bash
# Установи GitHub CLI и войди
gh auth login
```

## 📁 Альтернативный способ - через веб-интерфейс

### Если командная строка не работает:

1. **Создай репозиторий** на GitHub (как в шаге 2)
2. **Нажми "uploading an existing file"**
3. **Перетащи все файлы** из папки с ботом
4. **Напиши commit message**: "Initial commit - MCREW Design Bot"
5. **Нажми "Commit changes"**

## ✅ Проверка загрузки

### После успешной загрузки:

1. **Обнови страницу** репозитория на GitHub
2. **Проверь файлы**:
   - ✅ `bot.py` - основной файл бота
   - ✅ `requirements.txt` - зависимости
   - ✅ `Procfile` - для Render
   - ✅ `render.yaml` - конфигурация
   - ✅ `images/` - папка с изображениями
   - ✅ `README.md` - описание проекта

3. **Ссылка на репозиторий**: `https://github.com/YOUR_USERNAME/mcrew-design-bot`

## 🚀 Следующий шаг - Render

После загрузки на GitHub:
1. Перейди на https://render.com
2. Создай Web Service
3. Подключи свой GitHub репозиторий
4. Настрой переменные окружения
5. Деплой!

## 🔧 Команды для обновления бота

### Если нужно обновить код:

```bash
# Добавить изменения
git add .

# Коммит с описанием
git commit -m "Update bot features"

# Загрузить на GitHub
git push
```

## 📞 Помощь

### Если что-то не работает:

1. **Проверь Git**: `git --version`
2. **Проверь подключение**: `git remote -v`
3. **Статус файлов**: `git status`
4. **История коммитов**: `git log --oneline`

**Готово! Бот будет загружен на GitHub! 🎉**